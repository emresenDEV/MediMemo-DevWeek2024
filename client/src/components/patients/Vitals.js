import react from 'react';

// User can see a history of their vitals taken by the provider. 


function Vitals() {
    return (
        // JSX goes here
        //Time taken (time vitals were taken)
        //height (feet, inches?)
        //weight (lbs? kg?)
        //blood pressure (sbp[top num] and dbp[bottom num])
        //temperature (F? C?)
        //pulse (beats per minute: uses a home device to measure heart rate)
        //respiratory rate
        //oxygen saturation
        //pain level
        //blood glucose
        //symptom (checkboxes? Other: (user input))
        //comment
        //medication taken

        <></>
    )
}
export default Vitals;